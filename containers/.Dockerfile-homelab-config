FROM ghcr.io/gombos/rootfs-extra

# Setup tmp
COPY bin/* packages/* /tmp/
RUN true
COPY boot/ /tmp/
RUN true

# Change the workdir to tmp
WORKDIR /tmp

# Run
RUN ./infra-rootfs-config.sh

CMD ["/bin/bash"]
