FROM 0gombi0/homelab-baremetal:extra

# Setup tmp
COPY bin/* packages/* /tmp/
RUN true
COPY boot/ /tmp/
RUN true

# Change the workdir to tmp
WORKDIR /tmp

# Run
RUN ./infra-rootfs-config.sh

CMD ["/bin/bash"]
