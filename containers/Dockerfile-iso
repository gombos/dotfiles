ARG BASE=docker.io/ubuntu:jammy

FROM $BASE as base

#    - { tag: kernel    }
#                     - { tag: rootfs    }
#                     - { tag: usrlocal    }

FROM ghcr.io/gombos/rootfs as rootfs

ENV REPO=/_tmp

RUN --mount=target=$REPO $REPO/containers/infra-image.sh


