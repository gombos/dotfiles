# Base image could be independent from rootfs
FROM ubuntu:hirsute as base

# Use my own docker images as a base for testing
#FROM 0gombi0/homelab:desktop as base

LABEL name=efi

COPY bin/* /tmp/
COPY boot/* /tmp/
WORKDIR /tmp
RUN ./infra-build-efi.sh

FROM scratch
COPY --from=base /efi /efi
