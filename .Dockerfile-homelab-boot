# Base image could be independent from rootfs

FROM ubuntu:impish as base

LABEL name=efi

COPY bin/* /tmp/
COPY boot/ /tmp/
WORKDIR /tmp
RUN ./infra-build-boot.sh

FROM scratch
COPY --from=base /efi /efi
