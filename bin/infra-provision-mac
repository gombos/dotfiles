if [[ "$OSTYPE" != "darwin"* ]]; then
  exit
fi

softwareupdate --all --install --force

# install manually - xquartz, google-chrome, vmware fusion

brew update
brew upgrade

# brave - incognito browser
# cd Library/Preferences
# defaults write com.brave.Browser IncognitoModeAvailability -integer 2

# cask
brew install karabiner-elements	kitty skype	slack visual-studio-code brave-browser

#brew install osxfuse teamviewer veracrypt microsoft-edge google-cloud-sdk authy

brew install micro ripgrep-all rmlint qemu

#authy kitty microsoft-auto-update nomachine utm visual-studio-code brave-browser macfuse microsoft-edge osxfuse
#veracrypt karabiner-elements macs-fan-control microsoft-remote-desktop	teamviewer viber

# bitwarden hp-easy-scan vmware-fusion zoom.us
# https://alt-tab-macos.netlify.app/
# brew install --cask alt-tab

#    bash bash-completion pass pinentry-mac sshuttle lesspipe rclone lastpass-cli

# remoteviewer
# vlc
# xquartz
# microsoft-edge
# canon-eos-utility
# gimp
# syncthing
# nomachine

# basictex tesseract pandoc poppler wakeonlan

# macs-fan-control db-browser-for-sqlite libreoffice sshuttle rga

if [[ `hostname -s` != "taska" ]]; then
  brew install --cask slack
  brew upgrade --cask slack
fi

#python3 -m pip install --upgrade pip
#pip3 install visidata

brew cleanup --prune=all
brew doctor

#sudo pmset -a standby 0
# sudo pmset -a hibernatemode 0; sudo pmset -a disablesleep 1;

# The output of this command depends on weather on charger or off charger
#user:taska.local:~ sudo pmset -g
# standbydelaylow      10800
# standby              1
# halfdim              1
# hibernatefile        /var/vm/sleepimage
# gpuswitch            2
# powernap             0
# disksleep            10
# standbydelayhigh     86400
# sleep                1 (sleep prevented by sharingd)
# autopoweroffdelay    259200
# hibernatemode        3
# autopoweroff         1
# ttyskeepawake        1
# displaysleep         2
# highstandbythreshold 50
# acwake               0
# lidwake              1
# digikam