#!/bin/bash

#active_window_class=$(xprop -id $(xdotool getwindowfocus) | grep WM_CLASS | cut -d\" -f2)

run-or-raise.sh kitty.kitty "kitty"

#if [ "$active_window_class" = "kitty" ]; then
#    /bin/sh -c "kitty" &
#else
#  run-or-raise.sh kitty.kitty "kitty" &
#fi
